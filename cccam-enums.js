const PERSIST_FASTCAM_MODE = '@CCCamera:persistFastCamMode'; // whether user last used fast cam or preview mode
const PERSIST_FLASH_MODE = '@CCCamera:persistFlashMode'; // whether user last had the flash on or off
const PERSIST_RESOLUTION_MODE = '@CCCamera:persistResolutionMode'; // persist the last used resolutionMode

export {
  PERSIST_FASTCAM_MODE,
  PERSIST_FLASH_MODE,
  PERSIST_RESOLUTION_MODE,
};
